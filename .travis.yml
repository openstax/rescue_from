sudo: false
language: ruby
rvm:
  - "2.2.3"
cache: bundler
bundler_args: --retry=6
script:
  - bundle exec rake
notifications:
  email: false
addons:
   postgresql: "9.3"
before_script:
  - bundle exec rake db:create db:migrate
