require 'spec_helper'

describe OpenStax::RescueFrom do
  it 'has a version number' do
    expect(OpenStax::RescueFrom::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
